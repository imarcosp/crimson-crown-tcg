# Copy this file to .env.test.local only after `supabase start` succeeds.
# Populate the two key values exclusively from `supabase status --output env`.
# Never paste keys from .env, .env.local, .env.staging, or Supabase production.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54621
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000
